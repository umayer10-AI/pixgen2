import Banner from '@/component/Banner';
import { Button } from '@heroui/react';
import React from 'react';

const page = () => {
  return (
    <div>
        <Banner></Banner>
    </div>
  );
};

export default page;